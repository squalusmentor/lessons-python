from aiogram.fsm.state import State, StatesGroup


class StateMachine(StatesGroup):
    """Машина состояний бота.

    Состояние — это «на каком шаге диалога сейчас пользователь». Пока оно
    установлено, обычный текст пользователя попадает не в общий обработчик,
    а в тот, что зарегистрирован на это состояние. Промежуточные ответы
    (дата, время) складываем в хранилище состояния через state.update_data().
    """

    # === СОЗДАНИЕ ВСТРЕЧИ === #
    meeting_waiting_date = State()  # ждём дату в формате ДД.ММ.ГГГГ
    meeting_waiting_time = State()  # ждём время в формате ЧЧ:ММ
    meeting_waiting_text = State()  # ждём описание встречи

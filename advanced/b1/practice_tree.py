"""
Урок B1 — ПРАКТИЧЕСКАЯ РАБОТА 2 (Блок 2: рекурсия)

Свой аналог команды `tree`: рекурсивно обойти директорию и напечатать
дерево файлов и папок с отступами. Глубина вложенности заранее неизвестна —
поэтому рекурсия тут естественнее цикла.

Пример вывода (упрощённый, отступ пробелами):
    b1/
        readme.md
        main.py
        solutions/
            practice_tree_solution.py

Запуск:  python practice_tree.py
Решение (в т.ч. версия с рамками ├── └── │) — в solutions/practice_tree_solution.py.
"""

from pathlib import Path


# === TODO. print_tree ===
# Рекурсивно напечатай содержимое директории с отступом по глубине.
#
# Аргументы:
#   path  — Path к файлу или директории
#   depth — текущая глубина вложенности (для отступа), на старте 0
#
# Алгоритм:
#   1. Напечатай имя текущего элемента с отступом: "    " * depth + path.name
#      Для директории удобно добавить "/" в конце: path.name + "/".
#   2. БАЗА: если path — файл (path.is_file()), на этом всё, выходим.
#   3. ШАГ: если path — директория, перебери sorted(path.iterdir())
#      и для каждого entry вызови print_tree(entry, depth + 1).
#
# Подсказки:
#   - path.name       — имя файла/папки без пути
#   - path.is_dir()   — это директория?
#   - path.iterdir()  — итератор по содержимому директории
#   - sorted(...)     — чтобы порядок вывода был стабильным
#
# def print_tree(path: Path, depth: int = 0) -> None:
#     ...


def demo_tree():
    print("== Практика 2: аналог tree ==")
    here = Path(__file__).parent   # печатаем саму папку урока
    print(f"(корень: {here})")
    # print_tree(here)


if __name__ == "__main__":
    demo_tree()
